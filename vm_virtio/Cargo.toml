[package]
name = "vm_virtio"
version = "0.1.0"
edition.workspace = true

[dependencies]
virtio-queue = { workspace = true }
vm-memory = { workspace = true, features = [
  "backend-atomic",
  "backend-bitmap",
  "backend-mmap",
] }
